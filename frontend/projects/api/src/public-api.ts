/*
 * Public API Surface of api
 *
 * Domain models and signal-returning services live under `./lib/`. Each
 * vertical slice adds its model and service exports here.
 *
 * The package version constant exists so the entry isn't empty before the
 * first model lands in Phase 2; ng-packagr can't compile a zero-export
 * entry point.
 */

export const API_LIBRARY_VERSION = '0.0.0' as const;
