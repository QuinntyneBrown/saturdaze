# L2 - Detailed Requirements

> Each L2 traces to one L1 in `L1.md`. Acceptance criteria use Given/When/Then so they translate directly to tests. Every acceptance test in this repo must carry a `Traces to: L2-NNN` header comment.

---

## L2-001: User signup creates account and family
**Traces to:** L1-001

The system must accept a signup payload (email, password, optional family name, optional home location), create a `User` row, create an empty `Family` row owned by that user, issue an access token + refresh token, and return both with the user record.

**Acceptance Criteria:**
1. Given no user exists with email `new@example.com`, when a `POST /api/auth/register` is sent with `{email: "new@example.com", password: "Passw0rd!"}`, then the response status must be 201 and the body must contain a non-empty `tokens.accessToken`, a non-empty `tokens.refreshToken`, and a `user` with `emailVerifiedUtc=null`.
2. Given a user already exists with normalized email `new@example.com`, when registration is retried with the same email, then the response must be 409 with error code `email_in_use`.
3. Given any successful registration, when the database is inspected, then exactly one new `Family` row must exist with the new user's `FamilyId` pointing to it.
4. Given a password shorter than 8 characters, when registration is attempted, then the response must be 400 with a validation error naming the password field.
5. Given the signup form on `/signup` with terms unchecked, when the user fills email/password, then the submit button must remain disabled.

---

## L2-002: User login returns tokens and rotates refresh
**Traces to:** L1-001

The system must authenticate a user by `Email` + `Password`, issue a fresh access token + refresh token on every successful login, and return a generic 401 error on any credential failure (without distinguishing "no such user" from "wrong password").

**Acceptance Criteria:**
1. Given a seeded user `quinntynebrown@gmail.com` with password `password123`, when `POST /api/auth/login` is sent with those credentials, then the response must be 200 with `tokens.accessToken`, `tokens.refreshToken`, and `user.email` matching the seeded user.
2. Given the same user, when login is sent with a wrong password, then the response must be 401 with error code `invalid_credentials` and the response body must not reveal whether the email exists.
3. Given a successful login, when the database is inspected, then a new `RefreshToken` row must exist with `ExpiresAtUtc` 14 days in the future and `RevokedAtUtc` null.
4. Given a live refresh token, when `POST /api/auth/refresh` is sent with it, then the response must be 200 with a new access + refresh pair, the presented token must be revoked and linked to its replacement via `ReplacedByTokenId`, and presenting it again must return 401 `refresh_token_revoked`.

---

## L2-003: Sign-out revokes the active refresh token
**Traces to:** L1-001

A signed-in user must be able to sign out via a dialog from the profile page, after which their refresh token is revoked and stored token material is cleared from the client.

**Acceptance Criteria:**
1. Given the user is signed in, when they tap "Sign out" on `/profile` and confirm the dialog, then the client must clear access and refresh tokens from both `localStorage` and `sessionStorage`, and the next protected request must return 401.
2. Given an active refresh token at sign-out time, when the dialog is confirmed, then the client must call `POST /api/auth/logout` with it and the corresponding `RefreshToken.RevokedAtUtc` must be set in the database (the endpoint is anonymous and idempotent; it always returns 204).
3. Given the sign-out dialog is open, when the user presses Escape or taps the backdrop, then the dialog must close without signing the user out.

---

## L2-004: Forgot-password flow accepts an email
**Traces to:** L1-001

A signed-out user must be able to request a password-reset link by entering their email. The system must not reveal whether the email is registered.

**Acceptance Criteria:**
1. Given the user is on `/forgot-password`, when they submit a syntactically valid email, then they must be navigated to `/check-email` regardless of whether the email is registered.
2. Given an unregistered email is submitted, when the response is inspected, then it must be indistinguishable from a registered email submission (same status, same shape, same latency budget).
3. Given the email field is empty or malformed, when the user submits, then a field-level validation error must appear inline and the form must not be submitted.

---

## L2-005: Reset-password applies a new password via token
**Traces to:** L1-001

The system must accept a reset token plus a new password and confirmation, validate the token, enforce password complexity, and rotate any existing refresh tokens for that user on success.

**Acceptance Criteria:**
1. Given a valid, unexpired reset token, when the user submits matching passwords meeting complexity rules, then they must be redirected to `/login?reset=1` and the user's password hash must be updated.
2. Given an expired or invalid token, when the user submits, then an inline error must appear with a link back to `/forgot-password`, and the password hash must not change.
3. Given the password and confirmation fields differ, when the user submits, then a field-level "passwords do not match" error must appear and submission must be blocked.
4. Given a password shorter than 8 characters, or missing a number/symbol, when the user types, then an inline rule-checklist must indicate which rules are not yet satisfied.

---

## L2-006: Email verification links activate the account
**Traces to:** L1-001

The system must accept a verification token, mark the corresponding user as verified, and surface either a success or error state on `/verify-email`.

**Acceptance Criteria:**
1. Given a valid, unexpired verification token, when the user visits `/verify-email?token=<token>`, then `User.EmailVerifiedUtc` must be set to the current UTC time and the page must show a success state with primary CTA "Set up your family".
2. Given an invalid or expired token, when the user visits the page, then an error state must appear with a link to sign in or request a new link.
3. Given the user reaches the success state, when they tap "Skip — take me to the app", then they must be redirected to `/weekend`.

---

## L2-007: Remember-me toggle controls token persistence
**Traces to:** L1-001

The login form's "remember me" toggle must select between durable persistence (`localStorage`) and session-only persistence (`sessionStorage`) for the access and refresh tokens.

**Acceptance Criteria:**
1. Given remember-me is checked, when the user logs in successfully and then reloads the page, then the access token must still be present in `localStorage` and the user must remain on a protected route.
2. Given remember-me is unchecked, when the user logs in successfully, then `localStorage` must contain no Saturdaze token, `sessionStorage` must contain the access token, and closing the tab must end the session.

---

## L2-008: JWT-protected endpoints require a valid bearer token
**Traces to:** L1-001, L1-012

Every endpoint except `POST /api/auth/register|login|refresh|logout|forgot-password|reset-password|verify-email|resend-verification`, `GET /api/weather` (no PII), `GET /api/weekends/shared/{token}` and `GET /api/weekends/{id}/calendar.ics` (capability URLs that browsers and calendar clients fetch without a bearer) must reject requests that lack a valid `Authorization: Bearer <jwt>` header. `GET /api/activities|/api/restaurants|/api/events` are personalized per family (votes, locks, novelty history) and therefore require a bearer too (ADR-008). Enforcement is a global fallback authorization policy; anonymous routes opt out explicitly.

**Acceptance Criteria:**
1. Given no Authorization header, when `GET /api/auth/me` is requested, then the response must be 401.
2. Given an expired access token, when any protected endpoint is requested, then the response must be 401.
3. Given a token signed with the wrong key, when any protected endpoint is requested, then the response must be 401.
4. Given a valid bearer token, when `GET /api/auth/me` is requested, then the response must be 200 with the caller's `UserDto`.

---

## L2-009: Family profile view returns the caller's family
**Traces to:** L1-002

`GET /api/family` must return the authenticated caller's family with all members, commitments, and preferences in one response.

**Acceptance Criteria:**
1. Given an authenticated user whose family has 4 members and 3 commitments, when `GET /api/family` is requested, then the response must include exactly those 4 members and 3 commitments.
2. Given an authenticated user, when `GET /api/family` is requested, then the response must include `homeLocation`, `budgetEnabled`, members (id, name, age), commitments (id, title, dayOfWeek, startTime, endTime), and preferences (id, kind, value).
3. Given no authenticated user, when `GET /api/family` is requested, then the response must be 401.

---

## L2-010: Family profile edit persists changes
**Traces to:** L1-002

`PUT /api/family` must accept a full or partial family profile and persist additions, edits, and removals to members, commitments, and preferences.

**Acceptance Criteria:**
1. Given an existing family with members `[Quinn, Sara]`, when `PUT /api/family` is sent with members `[Quinn, Sara, Eli]`, then a subsequent `GET /api/family` must return 3 members including Eli with the supplied age.
2. Given an existing commitment "Swim 9–10am Sat", when `PUT /api/family` is sent omitting that commitment, then it must be removed from the family.
3. Given the profile page on `/profile`, when the user taps "Add member" then a CDK dialog must open (no inline form), and saving must call `PUT /api/family` exactly once with the new member appended.
4. Given an invalid payload (e.g., member with negative age), when `PUT /api/family` is sent, then the response must be 400 with a validation error.

---

## L2-011: Recurring commitments produce locked blocks
**Traces to:** L1-002, L1-004

Every `Commitment` on the family profile must materialize as a locked itinerary block on every generated weekend at the commitment's day and time window.

**Acceptance Criteria:**
1. Given a family with commitment "Swim 9:00–10:00 Saturday", when a new weekend is generated, then the Saturday timeline must contain a block titled "Swim" from 09:00 to 10:00 with `IsLocked=true` and `Kind=Commitment`.
2. Given a regenerate is requested for an existing weekend, when the response is returned, then every commitment block from the prior weekend must remain in place at the same time.

---

## L2-012: Generate weekend creates a Saturday+Sunday plan
**Traces to:** L1-003

`POST /api/weekends/plan` must create a new `Weekend` row for the requested weekend date with blocks across Saturday and Sunday, including activities, meals, drives, downtime, and any commitments from the family profile.

**Acceptance Criteria:**
1. Given an authenticated user with a family profile, when `POST /api/weekends/plan` is sent with `weekendOf=<saturday-date>`, then the response must be 200 with a `WeekendDto` whose `weekendOf` matches the input and whose `blocks` array spans both Saturday and Sunday.
2. Given the same family generates a plan twice for the same weekend, when the database is inspected, then exactly one `Weekend` row must exist for that family + date pair (uniqueness enforced).
3. Given no family profile data, when generation is attempted, then the response must succeed but produce only the bare-minimum blocks the system can construct, and not throw a 500.

---

## L2-013: Current weekend endpoint returns the upcoming plan
**Traces to:** L1-003

`GET /api/weekends/current` must return the family's plan for the current or next upcoming Saturday, generating one on demand if absent.

**Acceptance Criteria:**
1. Given an authenticated user with no weekends, when `GET /api/weekends/current` is requested, then a new `Weekend` for the upcoming Saturday must be created and returned with status 200.
2. Given a weekend already exists for the upcoming Saturday, when `GET /api/weekends/current` is requested, then the response must reference the existing weekend's ID (not create a new row).

---

## L2-014: Block lock toggle is idempotent
**Traces to:** L1-004

`PUT /api/blocks/{id}/lock` must set `ItineraryBlock.IsLocked` to the request value and return the parent weekend.

**Acceptance Criteria:**
1. Given an unlocked block, when `PUT /api/blocks/{id}/lock` is sent with `{locked: true}`, then the response must be 200 with the weekend showing that block's `isLocked=true`.
2. Given an already-locked block, when the same request is sent again, then the response must still be 200 and the block must remain locked (no error).
3. Given a block ID that does not exist, when the endpoint is called, then the response must be 404.
4. Given a block that belongs to another family, when the caller is not a member of that family, then the response must be 403 or 404 (no information leak).

---

## L2-015: Block swap replaces a slot and respects rejected suggestions
**Traces to:** L1-004

`POST /api/blocks/{id}/swap` must replace the activity referenced by a block with the next-best candidate, optionally excluding a list of rejected activity IDs supplied by the client.

**Acceptance Criteria:**
1. Given an Activity block referencing Activity A, when `POST /api/blocks/{id}/swap` is sent with `rejectedActivityIds=[A]`, then the response must contain that same block ID but with a different `refId` (not A).
2. Given a locked block, when swap is attempted, then the response must be 409 with error code `block_locked` and the block must remain unchanged.
3. Given the only remaining candidate has already been rejected, when swap is attempted, then the response must be 200 and the block must keep its current activity (no-op) with a `reason` annotation.

---

## L2-016: Regenerate preserves locks and increments RegenerateCount
**Traces to:** L1-004

`POST /api/weekends/{id}/regenerate` must rebuild every unlocked block, leave every locked block in place, and increment `Weekend.RegenerateCount`.

**Acceptance Criteria:**
1. Given a weekend with 10 blocks of which 3 are locked, when regenerate is called, then exactly those 3 locked blocks (by ID and content) must appear unchanged in the response, and the other 7 must have new content.
2. Given a weekend with `RegenerateCount=2`, when regenerate is called, then the database must show `RegenerateCount=3`.

---

## L2-017: Activity list applies filters and excludes age-misfits
**Traces to:** L1-005

`GET /api/activities` must accept query filters (`indoor`, `maxDriveMinutes`, `minAge`, `maxAge`, `weather`, `tryNew`, `take`) and return activities whose attributes satisfy every supplied filter.

**Acceptance Criteria:**
1. Given seeded activities, when `GET /api/activities?indoor=true` is requested, then every item in the response must have `indoor=true`.
2. Given seeded activities including one with `maxAge=8`, when `?minAge=9` is requested, then that activity must not appear in the response.
3. Given `?take=3`, when the request is sent, then the response must contain at most 3 items.
4. Given the activities page on `/activities`, when the user taps the "Outdoor" filter chip, then only outdoor activities must be displayed within the visible viewport without a full page reload.

---

## L2-018: Activities page groups results into three sections
**Traces to:** L1-005

The `/activities` page must render three labelled sections in order: weather-fit picks for the current forecast, "If weather turns" fallbacks, and "Try something new" novelty picks.

**Acceptance Criteria:**
1. Given the activities page is loaded, when the DOM is inspected, then exactly three `sd-section` elements must appear in document order with titles matching the three section names.
2. Given the forecast for Saturday is sunny, when the page loads, then the weather-fit section must contain at least one outdoor activity and at most one indoor activity (outdoor-first ordering).

---

## L2-019: Restaurants list scopes by day and meal slot
**Traces to:** L1-006

`GET /api/restaurants` must require a `day` and `slot` parameter, optionally narrow by `nearActivityId`, and default `wifeApprovedOnly=true`.

**Acceptance Criteria:**
1. Given a request without `day` or `slot`, when `GET /api/restaurants` is sent, then the response must be 400 with a validation error naming the missing field.
2. Given `?day=2026-05-23&slot=Lunch&wifeApprovedOnly=true`, when the request is sent, then every item in the response must have `wifeApproved=true` and a slot that includes lunch.
3. Given a seeded restaurant 6 minutes from a referenced activity, when `?nearActivityId=<id>` is supplied, then that restaurant must appear earlier in the list than restaurants ≥30 min away.

---

## L2-020: Local events filter by weekend and drive radius
**Traces to:** L1-007

`GET /api/events` must accept `weekendOf` and `maxDriveMinutes` (default 120) and return events whose start date falls between Friday of that weekend and the following Sunday inclusive.

**Acceptance Criteria:**
1. Given a weekend `weekendOf=2026-05-23`, when `GET /api/events?weekendOf=2026-05-23` is sent, then every returned event must have `startsOn` between 2026-05-22 (Fri) and 2026-05-24 (Sun) or be in a "coming soon" window of ≤14 days after.
2. Given `?maxDriveMinutes=30`, when the request is sent, then no returned event may have `driveMinutes > 30`.

---

## L2-021: Errand add returns updated weekend
**Traces to:** L1-008

`POST /api/weekends/{weekendId}/errands` must append a `ShoppingErrand` to the weekend, place an `Errand` block in the best free slot (the optional `preferredDay` first, then the other day; existing downtime is reclaimed), and return the updated `WeekendDto`.

**Acceptance Criteria:**
1. Given a weekend with zero errands, when `POST /api/weekends/{id}/errands` is sent with `{description: "Costco run", estimatedMinutes: 45}`, then the response must be 200 with `errands.length=1` and that errand's `done=false`.
2. Given the errand page on `/errand`, when the user submits the form with description + minutes + a "Best day" chip selected, then exactly one POST must be sent and the user must be navigated back to `/weekend` (or `/itinerary`) on success.

---

## L2-022: Errand done flag flips
**Traces to:** L1-008

`PUT /api/errands/{id}/done` must set the errand's `Done` field to the request value.

**Acceptance Criteria:**
1. Given an errand with `done=false`, when `PUT /api/errands/{id}/done` is sent with `{done: true}`, then a subsequent `GET` of the weekend must show that errand's `done=true`.
2. Given a non-existent errand ID, when the endpoint is called, then the response must be 404.

---

## L2-023: Weather endpoint returns Sat+Sun forecast for the family's home
**Traces to:** L1-009

`GET /api/weather?weekendOf=<date>` must return exactly two forecast entries (Saturday and Sunday) for the configured home location, with each entry exposing the date, the condition tags (`sunny`, `rain`, `snow`, `cold`, `cool`, `mild`, `warm`), high and low temperature, precipitation, and an `unavailable` flag; the client derives the day name, icon and note from those fields.

**Acceptance Criteria:**
1. Given `weekendOf=<a Saturday>`, when the endpoint is requested, then the response must be a JSON array of length 2 with the first entry's date equal to `weekendOf` and the second entry's date equal to `weekendOf + 1 day`.
2. Given the upstream provider returns 500, when the endpoint is requested, then the response must still be 200 with two neutral/unavailable forecast entries (no client-visible 5xx).
3. Given two requests for the same coordinates and date range within 60 minutes, when the second request fires, then it must be served from cache (verifiable by upstream call count = 1).

---

## L2-024: Weather provider is configurable and warns on placeholder key
**Traces to:** L1-009, L1-015

The weather provider base URL must come from configuration (`Saturdaze:Weather:BaseUrl`), and on production startup the system must log a warning if any production-required configuration value is unset or still holds its placeholder.

**Acceptance Criteria:**
1. Given the configuration sets `Saturdaze:Weather:BaseUrl="https://example.invalid/"`, when the API starts, then `OpenMeteoWeatherClient`'s `HttpClient.BaseAddress` must equal that URL.
2. Given `ASPNETCORE_ENVIRONMENT=Production` and `SATURDAZE_JWT_SIGNING_KEY` is the placeholder, when the API starts, then a warning log entry must be emitted naming the missing key.

---

## L2-025: Saved weekend history is paginated and filterable
**Traces to:** L1-010

`GET /api/weekends/history?take=N` must return up to N past weekends ordered by `WeekendOf` descending and include each weekend's title, rating, favourite flag, and a short highlights summary.

**Acceptance Criteria:**
1. Given 30 saved weekends, when `?take=10` is requested, then the response must contain exactly 10 items ordered newest-first.
2. Given a request with `?take=0`, when the endpoint is called, then the response must be 400 (zero is not a valid page size).
3. Given the saved page on `/saved`, when the user taps the "Favourites" filter chip, then only `IsFavourite=true` weekends must be displayed.

---

## L2-026: Weekend favourite and rating toggles persist
**Traces to:** L1-010

`PUT /api/weekends/{id}/favourite` must set `IsFavourite` to the request value; `PUT /api/weekends/{id}/rating` (`{ rating: 1..5 | null }`) must persist or clear a 1–5 star rating; `PUT /api/weekends/{id}/title` must persist the user-supplied title.

**Acceptance Criteria:**
1. Given a weekend with `isFavourite=false`, when `PUT /api/weekends/{id}/favourite` is sent with `{favourite: true}`, then a subsequent `GET` must show `isFavourite=true`.
2. Given a rating value outside the 1–5 range, when persistence is attempted, then a validation error must be returned and the existing rating must remain unchanged.

---

## L2-027: Avoid-repeating section flags low-rated activities
**Traces to:** L1-010

The `/saved` page must list activities that appeared in a past weekend rated ≤2 stars under an "Avoid repeating" section, with a "Skip" chip.

**Acceptance Criteria:**
1. Given a saved weekend rated 2 stars whose activities include "Rec Room", when the saved page is loaded, then "Rec Room" must appear under the "Avoid repeating" heading with a visible warn chip.
2. Given no saved weekend rated ≤2 stars, when the page is loaded, then the "Avoid repeating" heading must be hidden or replaced with an empty-state message.

---

## L2-028: All pages render correctly at every breakpoint
**Traces to:** L1-011

Every routed page (`/`, `/weekend`, `/ideas`, `/ideas/food`, `/ideas/events`, `/past`, `/family`, `/review-submissions`, `/legal`, `/sample-weekend`, all auth pages) must render usably at viewport widths 320 px, 390 px, 820 px, 1440 px, and 1920 px. (Revised 2026-09-02 with the v2 design, [ADR-009](../adr/ADR-009-v2-responsive-shell.md): the v1 split view is gone; the shell is a top bar from 720 px and a bottom nav below it.)

**Acceptance Criteria:**
1. Given viewport 320×568, when any page is rendered, then no element must overflow the horizontal viewport (no horizontal scroll).
2. Given viewport ≥720 px, when `/weekend` is rendered, then Saturday and Sunday must sit side by side (`.grid-days` two columns) under the top bar, with each day's header sticky at the top of its column and the bottom nav absent.
3. Given viewport <720 px, when `/weekend` is rendered, then the two days must stack into a single column, the bottom nav must be present and clear of the device chrome (ADR-005), and each block row must expose its details through the chevron rather than hover actions.
4. Given visual baselines captured from `docs/mocks-v2/` with `SD_BASELINE=1`, when the Playwright visual specs run, then each compared region must match its baseline within the configured pixel-diff tolerance (the tiers and masks are [ADR-010](../adr/ADR-010-visual-parity-policy.md)).

---

## L2-029: Passwords are stored as PBKDF2 hashes
**Traces to:** L1-012

Plaintext passwords must never be written to the database, and the password hash format must use PBKDF2 with at least 100,000 iterations and a per-user random salt.

**Acceptance Criteria:**
1. Given a user registers with password `Passw0rd!`, when the `Users` table is inspected, then the `PasswordHash` column must not contain the substring `Passw0rd!`.
2. Given two users register with the same password, when their hashes are compared, then the hashes must differ (per-user salting).

---

## L2-030: JWT signing key is loaded from environment, never committed
**Traces to:** L1-012

The JWT signing key must be read from the `SATURDAZE_JWT_SIGNING_KEY` environment variable in production and never embedded as a real value in source control.

**Acceptance Criteria:**
1. Given `SATURDAZE_JWT_SIGNING_KEY` is unset and `ASPNETCORE_ENVIRONMENT=Production`, when the API starts, then a warning log must indicate the signing key is using a placeholder.
2. Given the `appsettings.json` files in source control, when they are inspected, then the `Saturdaze:Jwt:SigningKey` value must be the literal placeholder string and not a real key.

---

## L2-031: CORS origins are explicit allowlist in production
**Traces to:** L1-012

In production, the API must accept cross-origin requests only from the explicit list configured in `Cors:AllowedOrigins`. Wildcard origins must be rejected.

**Acceptance Criteria:**
1. Given `Cors:AllowedOrigins=["https://allowed.example"]` and a CORS preflight from `Origin: https://allowed.example`, when `OPTIONS /api/auth/login` is sent, then the response must include `Access-Control-Allow-Origin: https://allowed.example`.
2. Given a preflight from `Origin: https://evil.example` and that origin is not configured, when `OPTIONS /api/auth/login` is sent, then the response must omit `Access-Control-Allow-Origin` (or return a 403).

---

## L2-032: All database access uses parameterized queries
**Traces to:** L1-012

The system must use EF Core LINQ or parameterized SQL for every database call; no string-interpolated SQL is permitted.

**Acceptance Criteria:**
1. Given the codebase, when grep is run for `FromSqlRaw`, `ExecuteSqlRaw`, or string-interpolated SQL inside C# files under `backend/src/`, then either zero matches must exist or every match must be wrapped with explicit parameter binding (not raw interpolation).

---

## L2-033: Refresh tokens rotate and track creator IP
**Traces to:** L1-012

Every issued refresh token must record `CreatedAtUtc`, `ExpiresAtUtc` (14 days), `CreatedByIp` (when available), and on use must be replaced by a new token with `ReplacedByTokenId` linking back.

**Acceptance Criteria:**
1. Given a user logs in twice in sequence, when the database is inspected, then two `RefreshToken` rows must exist with the second's `Id` referenced by the first's `ReplacedByTokenId` (or both alive if simultaneous-login policy permits multiple sessions).
2. Given a refresh token reaches `ExpiresAtUtc`, when it is presented to the refresh endpoint, then the response must be 401 and the user must be forced to log in again.

---

## L2-034: API responses meet latency budgets
**Traces to:** L1-013

Read endpoints must respond within 500 ms p95 on warm cache; write endpoints within 1000 ms p95; weekend generation within 2000 ms p95.

**Acceptance Criteria:**
1. Given a warmed API, when 50 sequential `GET /api/activities` requests are issued, then the p95 latency must be ≤ 500 ms.
2. Given a warmed API, when 20 sequential `POST /api/weekends/plan` requests are issued for different weekends, then the p95 latency must be ≤ 2000 ms.

---

## L2-035: Angular routes are lazy-loaded
**Traces to:** L1-013

Every routed page (other than the splash) must be lazy-loaded via `loadComponent`, so the initial bundle excludes those pages' code.

**Acceptance Criteria:**
1. Given a production `ng build`, when the build output is inspected, then `dist/saturdaze/browser/` must contain a separate lazy chunk for each of `/login`, `/signup`, `/weekend`, `/itinerary`, `/activities`, `/restaurants`, `/events`, `/errand`, `/saved`, `/profile`.
2. Given the initial bundle, when its size is measured, then it must respect the `500 kB warning / 1 MB error` budget configured in `angular.json`.

---

## L2-036: Every interactive element is keyboard reachable
**Traces to:** L1-014

Every button, link, form control, and dialog action must be reachable via Tab order and operable with Enter/Space, and the visible focus indicator must meet WCAG 2.1 AA contrast.

**Acceptance Criteria:**
1. Given any routed page, when a user tabs from the top of the document, then every interactive element must receive focus in a logical order and display a visible focus ring.
2. Given an open dialog, when the user presses Tab repeatedly, then focus must be trapped inside the dialog (not escape to the page behind it).
3. Given the user presses Escape while a dialog is open, then the dialog must close and focus must return to the element that triggered it.

---

## L2-037: Every meaningful element exposes an accessible name
**Traces to:** L1-014

Icon-only buttons must carry `aria-label`, form fields must have associated labels, and images carrying meaning must have descriptive `alt` text.

**Acceptance Criteria:**
1. Given any icon-only button (e.g., back arrow, refresh, calendar in the top bar), when inspected, then it must expose a non-empty accessible name via `aria-label` or visually hidden text.
2. Given any text input on the auth pages, when inspected, then it must have a programmatically associated label (matching `for` / `id` or `aria-labelledby`).

---

## L2-038: Color contrast meets WCAG 2.1 AA
**Traces to:** L1-014

All text, including chip labels and disabled controls used to convey state, must meet a 4.5:1 contrast ratio against its background (3:1 for large text ≥18 pt or 14 pt bold).

**Acceptance Criteria:**
1. Given any text node in any page, when axe-core is run, then no contrast violations must be reported.
2. Given any chip showing a state (locked, warn, primary), when its color combination is sampled, then its text/background ratio must be ≥ 4.5:1.

---

## L2-039: Structured logs are emitted for every request
**Traces to:** L1-015

Every API request must produce a Serilog log entry with method, path, status code, elapsed milliseconds, and (when authenticated) the user ID — but never the bearer token, password, or password hash.

**Acceptance Criteria:**
1. Given any request to `/api/auth/me`, when the log output is inspected, then a single request-completion entry must exist with `method=GET`, `path=/api/auth/me`, a status code, and an elapsed-ms value.
2. Given a login request, when the log output is inspected, then no log line must contain the submitted plaintext password or the issued access/refresh tokens.

---

## L2-040: Upstream provider failures degrade gracefully
**Traces to:** L1-009, L1-015

Upstream provider exceptions (HTTP, JSON, timeout) must be caught, logged at warning level, and substituted with a neutral fallback so the user never sees a 5xx caused by the upstream.

**Acceptance Criteria:**
1. Given Open-Meteo returns 503, when `GET /api/weather` is requested, then the API must respond 200 with two "unavailable" forecast entries and one warning-level log entry referencing the upstream failure.
2. Given a TaskCanceledException from Open-Meteo, when the same request is made, then the same fallback behavior must apply (no unhandled exception, no 5xx).

---

## L2-041: Pushes to main deploy API, run migrations, and deploy frontend
**Traces to:** L1-016

The `.github/workflows/deploy.yml` workflow must fire on every push to `main` and must produce, in order: a successfully deployed API on Azure App Service, applied EF migrations against Azure SQL, and a successfully deployed Angular bundle on Azure Static Web Apps.

**Acceptance Criteria:**
1. Given a commit is pushed to `main`, when the workflow completes, then all three jobs (`api`, `migrate`, `web`) must succeed.
2. Given the `migrate` job runs against an Azure SQL database where all migrations are already applied, when it executes, then the job must succeed without errors (idempotent).
3. Given the `web` job uploads the Angular build output, when the build output is inspected, then it must contain `staticwebapp.config.json` with a navigation fallback to `/index.html`.

---

## L2-042: SPA navigation fallback serves index.html for deep routes
**Traces to:** L1-011, L1-016

Refreshing any client-side route (e.g., `/login`, `/weekend`, `/itinerary`) on the Static Web App must return the SPA shell, not a 404.

**Acceptance Criteria:**
1. Given the deployed SWA, when `GET https://<swa-host>/login` is requested, then the response must be 200 and the body must contain `<app-root`.
2. Given the deployed SWA, when `GET https://<swa-host>/static/missing-asset.js` is requested, then the response must be 404 (asset-extension paths must not be rewritten).

---

## L2-043: `saturdaze migrate` applies EF migrations idempotently
**Traces to:** L1-017

The `saturdaze migrate` CLI command must apply pending EF Core migrations to the configured database and must be safe to run repeatedly.

**Acceptance Criteria:**
1. Given an empty database, when `saturdaze migrate --connection <cs>` is invoked, then every migration in `Saturdaze.Infrastructure/Migrations/` must be applied and the `__EFMigrationsHistory` table must list each one.
2. Given a database that already has all migrations applied, when the same command is invoked again, then the process must exit 0 with no schema changes.
3. Given the `--connection` flag is omitted, then the CLI must resolve the connection string from `SATURDAZE_CONNECTION` env var or `ConnectionStrings:Saturdaze` configuration before failing.

---

## L2-044: `saturdaze seed` is idempotent and scope-aware
**Traces to:** L1-017

The `saturdaze seed` CLI command must seed activities, restaurants, local events, the Brown family, and seed users from bundled JSON, must upsert rows by natural key (an existing row is refreshed from the JSON, never duplicated), and must accept a `--seed-dir` override.

**Acceptance Criteria:**
1. Given a freshly migrated database, when `saturdaze seed` is invoked, then the response must report inserted counts for each seeded entity and the database must contain those rows.
2. Given a database that already contains the seed rows, when `saturdaze seed` is invoked, then no duplicate rows must be inserted and the process must exit 0.
3. Given `--seed-dir <path>` pointing at a directory with overriding JSON files, when the command is invoked, then values from that directory must take precedence over bundled defaults.
4. Given neither `--seed-dir` nor `SATURDAZE_SEED_DIR`, when the command is invoked, then it must read the JSON bundled with the tool (never a previously copied per-user directory), so seed changes in the repo take effect on the next run.

---

## L2-045: `saturdaze reset` rebuilds a development database
**Traces to:** L1-017

The `saturdaze reset` CLI command must drop the schema, re-apply migrations, and re-seed, gated behind a confirmation step (or `--yes`).

**Acceptance Criteria:**
1. Given any non-empty development database, when `saturdaze reset --yes --connection <cs>` is invoked, then on completion the database must contain only the migrated schema plus the bundled seed data.
2. Given the command is invoked without `--yes`, then it must refuse with a non-zero exit code and change nothing (the CLI is non-interactive; there is no prompt).
3. Given `DOTNET_ENVIRONMENT` or `ASPNETCORE_ENVIRONMENT` is `Production`, then the command must refuse to run (exit code 5) unless `--allow-production` is passed, to prevent accidental production resets.

---

## L2-046: Authenticated users can submit a community event
**Traces to:** L1-018

`POST /api/events/submissions` must accept a payload from an authenticated user, persist an `EventSubmission` row with `status="pending"` and `submittedByUserId` set to the caller, and return the created submission. The payload must require `title`, `startsAtLocal` (ISO 8601 local date-time), and optionally accept `location`, `description`, `costNote`, `ageRange`, and `sourceUrl`. Pending submissions must not appear in `GET /api/events` responses for any user.

**Acceptance Criteria:**
1. Given an authenticated user, when `POST /api/events/submissions` is sent with `{title: "Port Credit Buskerfest", startsAtLocal: "2026-06-20T14:00"}`, then the response must be 201, the body must include `status="pending"`, and a `GET /api/events?weekendOf=2026-06-20` from any other user must not include that title.
2. Given an unauthenticated request, when the same endpoint is called, then the response must be 401.
3. Given a payload missing `title` or `startsAtLocal`, when the endpoint is called, then the response must be 400 with a validation error naming the missing field(s).
4. Given a `sourceUrl` that is not a syntactically valid http(s) URL, when the endpoint is called, then the response must be 400 with error code `invalid_url`.
5. Given any successful submission, when the database is inspected, then the row must record `submittedByUserId`, `submittedAtUtc`, and `status="pending"`.

---

## L2-047: Event submission form defaults date and time to next hour
**Traces to:** L1-018

The "Suggest an event" dialog (D10 in `docs/mocks-v2`; the only submission surface since the v2 design, see [ADR-009](../adr/ADR-009-v2-responsive-shell.md)) must default the date+time field to one hour from now, rounded up to the next whole hour in the user's local time zone, and must disable the submit button until `title` is non-empty.

**Acceptance Criteria:**
1. Given the current local time is 2026-05-18 11:23, when the user opens the submit form, then the default value of the date-time field must be `2026-05-18T13:00` (now + 1h, snapped up to the next whole hour).
2. Given the current local time is exactly 2026-05-18 11:00:00, when the user opens the submit form, then the default must be `2026-05-18T12:00` (the rule snaps up even when already on the hour, to preserve the +1h offset).
3. Given the form is freshly opened with only the default date-time, when no title is entered, then the "Submit event" button must be disabled and announce `aria-disabled="true"`.
4. Given a non-empty `title` is entered, when all other fields are left blank, then the submit button must become enabled and submitting must succeed (per [L2-046](#l2-046-authenticated-users-can-submit-a-community-event)).
5. Given the user clears the date-time field, when they attempt to submit, then an inline validation error must appear on the field and submission must be blocked.

---

## L2-048: Submission entry points exist on the events feed
**Traces to:** L1-018

`/ideas/events` must surface one affordance to submit a new event: the primary "Suggest an event" button in the page header, which opens the "Suggest an event" dialog (D10) in place. On success the "Thanks, it is in the queue" dialog (D11) summarises the submission, and the feed shows it under "Your suggestion" with a "Pending review" chip. (Revised 2026-09-02 with the v2 design: the floating "+" button and the dedicated `/events/submit` screen were retired, and `/events`, `/events/submit` and `/events/submitted` redirect to `/ideas/events` — see [ADR-009](../adr/ADR-009-v2-responsive-shell.md).)

**Acceptance Criteria:**
1. Given the user is on `/ideas/events`, when the page is rendered at any width, then the header must show a primary button named "Suggest an event", and it must be the only submission affordance on the page.
2. Given the user activates "Suggest an event", when the dialog opens, then it must be a CDK dialog rendered as a bottom sheet below 720 px and a centred modal from 720 px, with the title field focused.
3. Given the dialog submits successfully, when the response arrives, then the D11 confirmation must open with the submitted title, start date and "Pending review", and closing it must leave the user on `/ideas/events` with the submission listed under "Your suggestion".
4. Given the user opens `/events/submit` or `/events/submitted` from an old link, when the route resolves, then the app must redirect to `/ideas/events`.

---

## L2-049: Pending submissions are visible to their submitter only
**Traces to:** L1-018

`GET /api/events/submissions/mine` must return the caller's own submissions in any status (`pending`, `approved`, `rejected`). The events feed at `/events` must render a "Pending review" badge on cards drawn from the caller's own pending submissions so they understand the event isn't yet public.

**Acceptance Criteria:**
1. Given the user has submitted an event that is still pending, when `GET /api/events/submissions/mine` is called, then the response must include that submission with `status="pending"`.
2. Given another authenticated user, when they call `GET /api/events/submissions/mine`, then the first user's pending submission must not appear in the response.
3. Given the events page on `/events`, when the submitter loads the page, then their own pending submission must render as an `sd-event-card` with a `Pending review` chip and must not be tappable to "Add to weekend".
4. Given a non-submitting user loads `/events`, when the page renders, then no pending submission belonging to anyone else must be present in the DOM.

---

## L2-050: Admin moderation queue approves or rejects submissions
**Traces to:** L1-018

Users with the `admin` role must have access to `/admin/events` listing all pending submissions. Approving a submission must move its `status` to `approved` and copy it into the public events catalogue; rejecting must move its `status` to `rejected` and record an optional `rejectionReason`. Non-admins must receive 403 when calling the moderation endpoints.

**Acceptance Criteria:**
1. Given an admin user with one pending submission, when they `POST /api/events/submissions/{id}/approve`, then the submission's `status` must become `approved` and the event must subsequently appear in `GET /api/events` for any user whose weekend covers its date.
2. Given an admin user, when they `POST /api/events/submissions/{id}/reject` with `{reason: "duplicate of seeded event"}`, then `status` must become `rejected` and `rejectionReason` must be persisted.
3. Given a non-admin authenticated user, when they call either moderation endpoint, then the response must be 403 with error code `forbidden`.
4. Given an admin loads `/admin/events`, when the page renders, then it must list every `pending` submission with submitter email, submitted timestamp, and the event's proposed title/date/time, sorted oldest-first.
5. Given the admin taps "Reject" on a row, when the confirmation dialog opens and they enter a reason and confirm, then exactly one `POST /api/events/submissions/{id}/reject` call must be made and the row must disappear from the queue on success.

---

## L2-051: Single-source design token vocabulary
**Traces to:** L1-019

`design-system/assets/tokens.css` must declare the complete `--sd-*` token vocabulary on `:root`: the warm-neutral color palette, the Inter-first font stack, a seven-step type scale, a ten-step four-pixel spacing scale, five radii, three elevation shadows, motion easing and durations, layout dimensions, and the two application breakpoints. It must be the only file in the folder that declares the token vocabulary, and the only responsive override must be a single desktop media query adjusting padding and the three largest type sizes.

**Acceptance Criteria:**
1. Given the built stylesheet, when `:root` is inspected, then `--sd-bg` must be `#FAF7F2`, `--sd-primary` must be `#E07856`, `--sd-accent` must be `#2D7D5F`, and `--sd-warn` must be `#C45A3F`, with matching `-soft` washes and the four chip colors `--sd-sun`, `--sd-sky`, `--sd-leaf`, `--sd-indoor` defined.
2. Given the type and spacing scales, when computed on the root element below 1024 px, then `--sd-fs-xs` through `--sd-fs-xxl` must resolve to 12, 13, 15, 17, 20, 26, and 34 px, and `--sd-s-0` through `--sd-s-9` must resolve to 0, 4, 8, 12, 16, 20, 24, 32, 40, and 56 px.
3. Given radii, elevation, and motion tokens, when inspected, then `--sd-r-sm|md|lg|xl|pill` must be 8, 12, 16, 22, and 999 px, exactly `--sd-shadow-1`, `--sd-shadow-2`, and `--sd-shadow-3` must be defined, and `--sd-dur-fast` and `--sd-dur-base` must be `120ms` and `220ms` with easing `--sd-ease`.
4. Given the breakpoint tokens, when inspected, then `--sd-bp-tablet` must be `720px` and `--sd-bp-desktop` must be `1024px`, and a `@media (min-width: 1024px)` block must raise `--sd-app-pad-x` to 32px, `--sd-fs-xxl` to 40px, `--sd-fs-xl` to 30px, and `--sd-fs-lg` to 22px.
5. Given all validated files in the folder, when `npm run validate` scans them, then exactly one file must match the token-declaration pattern for `--sd-bg`; a second declaring file must fail validation with `Expected one token declaration file, found 2.`.

---

## L2-052: Custom element registration and naming
**Traces to:** L1-020

`design-system/assets/components.js` must import exactly the component source files named in the manifest, each of which self-registers its element(s) via `customElements.define`. All 29 public selectors must be unique, match `^sd-[a-z0-9-]+$`, attach open shadow roots, and belong to one of the seven manifest categories. `sd-base.js` is shared infrastructure and must not be a public catalog entry.

**Acceptance Criteria:**
1. Given the registry `assets/components.js`, when its `./components/sd-*.js` imports are compared with the manifest's `source` values, then the two sets must be identical in both directions (no unmanifested import, no unimported source).
2. Given any catalog page has loaded, when `customElements.get(selector)` is evaluated for each of the 29 manifested selectors, then every call must return a defined constructor — including both `sd-weather-strip` and `sd-weather-day`, which share the source file `sd-weather-strip.js`.
3. Given any upgraded catalog element, when inspected, then `element.shadowRoot` must be non-null (open shadow root).
4. Given the manifest, when each component's `category` is checked, then it must be one of `actions`, `forms`, `surfaces`, `navigation`, `planning`, `discovery`, `overlays`, and each component's `selector` must match `^sd-[a-z0-9-]+$` with no duplicates.
5. Given `sd-base.js`, when the manifest and registry are inspected, then it must appear in neither (it is imported only by component sources).

---

## L2-053: Modal dialog element behavior
**Traces to:** L1-020

`sd-dialog` must implement complete modal behavior in the component itself: focus capture and restoration, Tab cycling across shadow boundaries, Escape and backdrop dismissal via a cancelable `sd-close` event, an inline `static` presentation for documentation, and responsive bottom-sheet-to-centered-modal presentation at the 720 px breakpoint.

**Acceptance Criteria:**
1. Given an open non-static `sd-dialog`, when its sheet is inspected, then it must expose `role="dialog"`, `aria-modal="true"`, and `aria-labelledby="dialog-title"` naming it after its `title` attribute; given the `static` attribute, then the sheet must render inline with `role="region"` and no fixed scrim.
2. Given focus is on the last focusable item inside an open dialog, when `Tab` is pressed, then focus must wrap to the first focusable item, and `Shift+Tab` from the first must wrap to the last — where focusables are resolved through child shadow roots (`sd-button`'s inner `button`); given no focusable content, then the sheet itself (`tabindex="-1"`) must receive focus.
3. Given an open dialog, when `Escape` is pressed or the scrim itself is clicked, then the dialog must dispatch `sd-close` (bubbling, composed, cancelable) with `detail.reason` of `escape` or `backdrop` respectively and then remove its `open` attribute; given a listener calls `preventDefault()` on `sd-close`, then `open` must remain.
4. Given the element that was focused when the dialog opened is still connected, when the dialog closes, then that element must regain focus.
5. Given a viewport narrower than 720 px, when a non-static dialog opens, then it must present as a bottom sheet (top-only radius, bottom-aligned scrim); given a viewport of at least 720 px, then it must present as a centered modal with `max-width: 520px`.

---

## L2-054: Manifest schema, counts, and versioning
**Traces to:** L1-021

`design-system/component-manifest.json` must declare `schemaVersion` 3 and a `product.version` equal to the folder's `package.json` version, and must inventory exactly 29 components, 7 categories, 7 dialog families totalling 18 scenarios, and 8 pattern families totalling 40 responsive states, with complete and unique family and scenario metadata.

**Acceptance Criteria:**
1. Given the manifest, when parsed, then `schemaVersion` must equal `3` and `product.version` must equal the `version` in `design-system/package.json`; a mismatch must fail `npm run validate` with `Manifest and package versions must match.`.
2. Given the manifest, when counted, then `components` must have exactly 29 entries, `dialogs` exactly 7, and `patterns` exactly 8, and the summed `scenarios` across dialogs and patterns must be exactly 18 and 40 respectively.
3. Given any dialog or pattern family, when inspected, then it must carry non-empty `id`, `name`, and `description`, every scenario must carry non-empty `id` and `name`, and scenario ids must be unique within the family.
4. Given the per-family distribution, when counted, then the `product-action` dialog family must contain 12 scenarios and the `authentication` pattern family must contain 15 states.

---

## L2-055: Manifest-to-code parity
**Traces to:** L1-021

Every manifest component entry must be verifiably consistent with its source: the source file must exist, every attribute a component observes in code must be documented, and every documented API shape must be complete enough to render the reference pages and playground without guesswork.

**Acceptance Criteria:**
1. Given any component entry, when validated, then its `source` must match `^sd-[a-z0-9-]+\.js$` and exist in `assets/components/`.
2. Given any component source file, when its `observedAttributes()` arrays are extracted, then every observed attribute name must be documented in the manifest for a component sharing that source; an undocumented one must fail validation with `<source> observes undocumented attribute: <name>.`.
3. Given any component entry, when validated, then `attributes`, `slots`, `events`, and `examples` must all be arrays, there must be at least one example, and every example must carry `id`, `title`, `description`, and markup containing `<` followed by the component's own selector.
4. Given any documented attribute, when validated, then it must carry `type`, `description`, and a `default` key; attributes of type `enum` must declare a non-empty `values` list; and attribute names must be unique per component.

---

## L2-056: Client-side routing and deep links
**Traces to:** L1-022

The documentation app must intercept same-origin `a[data-route]` clicks, push history entries, and re-render on `popstate`, so `/`, `/foundations`, `/components[/:selector/:tab]`, `/dialogs[/:id/:tab]`, and `/patterns[/:id/:tab]` are all directly addressable, survive a browser refresh, and fall back to an in-app not-found page for unknown paths.

**Acceptance Criteria:**
1. Given the user clicks a sidebar or card link, when navigation occurs, then no full page load must happen (`history.pushState`), the window must scroll to the top, and focus must move to `#main`.
2. Given a direct browser load or reload of `/components/sd-dialog/api`, when the page renders, then the Dialog component's API tab must appear (the `Attributes` heading is visible), and `document.title` must be `Dialog · Saturdaze Design System`.
3. Given an unknown path such as `/nope`, when rendered, then the in-app error page must show the eyebrow `404 · NOT FOUND`, the heading `That page wandered off.`, and a `Return to the design system` link; and a static `/404.html` document must exist for host-level 404s.
4. Given the browser back button is pressed after client-side navigation, when `popstate` fires, then the previous route must re-render without a full page load.
5. Given the current URL, when the sidebar renders, then the exactly-matching link must carry `aria-current="page"`, and section links (`/components`, `/dialogs`, `/patterns`, `/foundations`) must carry the `active` class for any descendant route.
6. Given a detail route with an unrecognized trailing tab segment, when rendered, then the `overview` tab must be shown (tab segment falls back rather than 404ing).

---

## L2-057: Home page summarizes live catalog coverage
**Traces to:** L1-022

The home route `/` must present the product hero, four coverage metrics computed from the manifest at render time (never hard-coded), and three curated sample sections linking into components, patterns, and dialogs.

**Acceptance Criteria:**
1. Given `/` renders, when inspected, then the h1 must read `Warm, useful interfaces for weekends together.` and the eyebrow must include the manifest product version (`FIRST-CLASS PRODUCT · v2.0.0`).
2. Given the `Catalog coverage` region, when inspected, then it must show four metrics — 29 `live components`, 18 `dialog scenarios`, 8 `pattern families`, 40 `responsive states` — each derived from `manifest.components.length`, summed dialog scenarios, `manifest.patterns.length`, and summed pattern states.
3. Given the three sample sections, when counted, then exactly 3 `[data-component-card]` (featuring `sd-button`, `sd-dialog`, `sd-activity-card`), 3 `[data-pattern-card]`, and 3 `[data-dialog-card]` entries must render, each linking to its detail route.

---

## L2-058: Mobile navigation drawer
**Traces to:** L1-022

Below the documentation-shell breakpoint the sidebar must become an off-canvas drawer opened by a hamburger button, with scrim, body scroll lock, keyboard dismissal, and focus return; at and above the breakpoint the sidebar must be persistent. Crossing the breakpoint must never animate the closed drawer across the page or strand open-drawer state.

**Acceptance Criteria:**
1. Given a 390 px viewport, when `Enter` is pressed on the `#menu` button, then `#docs-nav` must gain the `open` class, `#menu` must switch to `aria-expanded="true"` with label `Close documentation navigation`, the `#nav-scrim` must appear, and `body` must gain `nav-open` (scroll locked via `overflow: hidden`).
2. Given the drawer is open, when a navigation link is activated, then the route must change and the drawer must close.
3. Given the drawer is open, when `Escape` is pressed, then the drawer must close and focus must return to `#menu`; clicking the scrim must also close it.
4. Given a viewport of at least 860 px, when the shell renders, then the hamburger must be hidden and the navigation must render as a persistent sticky sidebar.
5. Given the drawer is closed, when the viewport crosses the 860 px breakpoint in either direction, then the navigation must not animate or appear over page content — the closed drawer must carry no `transition` until the user first opens it (armed via `body.nav-armed`).
6. Given the drawer is open at a narrow width, when the viewport widens past 860 px, then the drawer state must be cleared: `#nav-scrim` hidden, the `body` scroll lock removed, `#menu` reset to `aria-expanded="false"`, and the navigation restored as the persistent sidebar.

---

## L2-059: Foundations reference renders from live tokens
**Traces to:** L1-023

`/foundations` must document the token vocabulary with specimens rendered from the running stylesheet, in six sections, so the page cannot disagree with `tokens.css`.

**Acceptance Criteria:**
1. Given `/foundations` renders, when inspected, then exactly six section headings must appear: `Color`, `Typography`, `Spacing`, `Shape & elevation`, `Motion & breakpoints`, `Icons`.
2. Given the Color section, when counted, then exactly 18 `.token-card` entries must render, each swatch styled with `background: var(--sd-<token>)` and labelled with the token name in a `code` element.
3. Given the Typography section, when inspected, then 7 specimen rows (`--sd-fs-xxl` down to `--sd-fs-xs`) must render, each sample styled with `font-size: var(--sd-fs-<token>)`.
4. Given the Spacing section, when inspected, then the ten `--sd-s-0` … `--sd-s-9` cards must display values read at render time via `getComputedStyle(document.documentElement).getPropertyValue(...)`, not hard-coded strings.
5. Given the Shape & elevation and Motion & breakpoints sections, when inspected, then five radius cards, three shadow cards (each styled with `box-shadow: var(--sd-shadow-N)`), two duration cards (`120ms`, `220ms`), and two breakpoint cards (`720px`, `1024px`) must render.
6. Given the Icons section, when counted, then exactly 39 `.icon-card` entries must render, each containing a live `sd-icon` and its name in a `code` element.

---

## L2-060: Component index grouped by category
**Traces to:** L1-024

`/components` must render one live card for every manifested component, grouped under the seven category headings in manifest order, each card previewing the real upgraded element and linking to its detail page.

**Acceptance Criteria:**
1. Given `/components` renders, when counted, then exactly 29 `[data-component-card]` entries must appear under the page eyebrow `29 NATIVE CUSTOM ELEMENTS`, and every card's `.card-preview` must contain at least one rendered child element.
2. Given the category sections, when inspected, then each of the seven categories must render its label and description as a section heading with its member components' cards beneath it.
3. Given any card, when its link is activated, then the app must navigate to `/components/<selector>/overview`.

---

## L2-061: Component detail overview and API tabs
**Traces to:** L1-024

Every component detail page must carry breadcrumbs, a header with category eyebrow and selector badge, and a three-tab `Page sections` navigation. The overview tab must give usage guidance plus a contract summary and canonical specimen; the API tab must tabulate the manifest contract with explicit empty states and link to the raw source.

**Acceptance Criteria:**
1. Given `/components/sd-button/overview`, when rendered, then the breadcrumb must read Overview / Components / Button (terminal crumb `aria-current="page"`), the header must show the category label eyebrow, the h1 `Button`, and the selector badge `<sd-button>`, and the `Page sections` navigation must contain three tab links whose hrefs end in `/overview`, `/api`, `/examples`, with the current tab carrying the `active` class.
2. Given the overview tab, when inspected, then prose sections `When to use`, `Composition`, `Accessibility`, and `Responsive behavior` must render alongside a `Contract at a glance` card listing the documented attribute, slot, and curated-example counts plus `Native custom element` and `Open shadow root`, followed by a `Canonical specimen` section rendering `examples[0]` with an `Open playground →` link.
3. Given the API tab, when inspected, then an `Attributes` table with columns Name, Type, Values, Default, Description, a `Slots` table, and an `Events` table must render from the manifest; enum values must render as `code` chips and missing values/defaults as `—`.
4. Given a component with an empty contract array, when the API tab renders, then the corresponding table must be replaced by the exact empty state: `This component has no public host attributes.`, `This component has no content slots.`, or `This presentation component does not dispatch a custom event.`.
5. Given the API tab's `Styling contract` section, when inspected, then it must state that the component consumes `--sd-*` tokens in an open shadow root and must link `Open local source ↗` to `/assets/components/<source>` in a new tab.
6. Given a tab link is clicked, when the tab changes, then the URL must update to `/components/<selector>/<tab>` via client-side navigation.

---

## L2-062: Interactive playground, synced code panels, and variant matrix
**Traces to:** L1-024

The examples tab must render a playground that mutates a live element instance through per-attribute controls, a code panel whose HTML tab tracks playground state and supports copy-to-clipboard, a reset action, and an auto-generated variant matrix covering every enumerated and boolean option.

**Acceptance Criteria:**
1. Given a component's examples tab, when the playground renders, then it must contain one control per documented attribute: a checkbox for `boolean`, a select of the declared `values` for `enum`, a select of all 39 icon names for `icon`, a number input for `number`, and a text input otherwise, each initialized from the canonical example's current attribute state.
2. Given the `sd-button` playground, when `variant` is set to `danger`, then the live preview element must carry `variant="danger"` and the code panel's visible HTML must contain `variant="danger"`; when the `disabled` checkbox is checked, then the element must carry the bare `disabled` attribute; when a control is cleared to empty, then the attribute must be removed.
3. Given playground edits have been made, when `Reset` is clicked, then the preview must be restored to the original example markup, every control must return to its initial value, and `Playground reset` must be announced.
4. Given the code panel, when inspected, then it must expose a `role="tablist"` labelled `Example source` with `HTML`, `CSS`, and `JavaScript` tabs toggling `aria-selected`, and a `Copy` button that writes the active tab's source via `navigator.clipboard.writeText` and announces `HTML copied` (the active tab name uppercased).
5. Given the variant matrix, when rendered, then it must contain one card per enum value per enum attribute plus a `false` and `true` card per boolean attribute (excluding `open` and `static`), each labelled `<attribute>: <value>`; `sd-icon` must instead render the 39-icon grid; and a component with no variant-producing attributes must fall back to a single `Canonical specimen` card.
6. Given each curated manifest example, when the examples tab renders, then it must appear as its own showcase with title, description, rendered specimen, and its own code panel.

---

## L2-063: Dialog family catalog pages
**Traces to:** L1-025

`/dialogs` must render one card per family, each containing a real inline static dialog specimen, and every family detail page must render all of its scenarios inline with no text-only placeholders.

**Acceptance Criteria:**
1. Given `/dialogs` renders, when counted, then exactly 7 `[data-dialog-card]` entries must appear under the eyebrow `7 FAMILIES · 18 SCENARIOS`, and each card's preview must contain an `sd-dialog[static][open]` element.
2. Given a family detail route `/dialogs/<id>/<tab>`, when rendered, then the header must show the eyebrow `<N> RENDERED SCENARIO(S)`, the badge `sd-dialog`, and the three-tab navigation.
3. Given `/dialogs/product-action/examples`, when counted, then exactly 12 `.dialog-example` entries must render, each containing an `sd-dialog[static][open]` specimen and a `Launch live` button, including the scenarios headed `Calendar export` and `Share fallback`.
4. Given a family's API tab, when inspected, then a `Scenarios` table (columns `Stable ID`, `Rendered composition`) must list every scenario id, and a `Base component` table must document the `open` attribute, the `static` attribute, and the `actions` slot.
5. Given a family's overview tab, when rendered, then exactly the first scenario must render inline with a `View all examples →` link to the examples tab.

---

## L2-064: Live dialog launcher lifecycle
**Traces to:** L1-025

`Launch live` must inject a working modal for the chosen scenario into `#dialog-host`, move focus into it, and support every dismissal path with focus restoration and a status announcement.

**Acceptance Criteria:**
1. Given `/dialogs/sign-out/examples`, when `Launch live` is clicked, then a live (non-static) dialog with accessible name `Sign out?` must appear in `#dialog-host`, focus must move to the first `sd-button`'s inner shadow button on the next animation frame, and `Dialog opened` must be announced.
2. Given the live dialog is open, when `Escape` is pressed, then the dialog must be removed (`#dialog-host` emptied) and focus must return to the `Launch live` button.
3. Given the live dialog is open, when the scrim itself is clicked, then the dialog must close with the same focus restoration.
4. Given a button inside the dialog carries `data-dialog-close` or `data-dialog-confirm`, when it is clicked (matched via `composedPath()` across shadow boundaries), then the dialog must close and `Dialog cancelled` or `Action confirmed` respectively must be announced.
5. Given the dialog closes via the component's own `sd-close` event with no marked button, when the host still contains the dialog, then the launcher must clear the host and announce `Dialog closed`.

---

## L2-065: Pattern family catalog pages
**Traces to:** L1-026

`/patterns` must render one card per family with an iframe preview, and each family's examples tab must render one framed specimen per declared responsive state, so all 40 states are reachable.

**Acceptance Criteria:**
1. Given `/patterns` renders, when counted, then exactly 8 `[data-pattern-card]` entries must appear under the eyebrow `8 FAMILIES · 40 STATES`, each embedding an iframe of `/preview.html?type=pattern&item=<id>&scenario=<first-scenario>`.
2. Given a family detail route, when rendered, then the header must show the eyebrow `<N> RESPONSIVE STATE(S)`, the badge `Product pattern`, and a two-tab navigation (`overview`, `examples` only).
3. Given `/patterns/authentication/examples`, when counted, then exactly 15 `.pattern-card` entries with iframes must render — one per declared state — and every family's examples tab must render exactly its declared state count.
4. Given a family's overview tab, when rendered, then a `States included` list must name every scenario and a representative rendering of the first state must appear with a `View all <N> states →` link.

---

## L2-066: Pattern viewport switcher drives real media queries
**Traces to:** L1-026

Every pattern specimen must carry viewport controls that resize the iframe itself, so the document inside responds through its real 720/1024 px media queries rather than simulated styles.

**Acceptance Criteria:**
1. Given any `.pattern-card`, when inspected, then a `Preview viewport` control group must render three buttons labelled `390`, `820`, and `Full` with aria-labels `Mobile viewport`, `Tablet viewport`, and `Desktop viewport`, with desktop active by default.
2. Given the `Mobile viewport` button is clicked, when the card updates, then that card's iframe must carry `data-viewport="mobile"` (constraining its width to 390 px) while other cards' controls are unaffected, and `Mobile preview selected` must be announced.
3. Given the iframe width changes, when the embedded document lays out, then its own `min-width: 720px` / `min-width: 1024px` media queries must engage or release accordingly (no viewport simulation in the parent document).

---

## L2-067: Isolated specimen page
**Traces to:** L1-026

`preview.html` must render a single chrome-free specimen selected by query string — a pattern state, dialog scenario, or component example — for use inside catalog iframes and by direct navigation.

**Acceptance Criteria:**
1. Given a direct load of `/preview.html?type=pattern&item=weekend-overview&scenario=default`, when rendered, then a `.product-screen` composition containing a live `sd-hero` must appear with no documentation header, sidebar, or search present in the DOM.
2. Given the `type` parameter is `pattern`, `dialog`, or `component`, when the page renders, then the corresponding fixture builder (`patternMarkup`, `dialogMarkup`, `componentMarkup` from `assets/catalog-content.js`) must produce the markup and the root element must carry a matching `data-type`.
3. Given `staticwebapp.config.json`, when its `navigationFallback.exclude` list is inspected, then `/preview.html` must be excluded so the file is served as a real static document rather than rewritten to the SPA shell.
4. Given anchors inside a specimen, when clicked, then navigation must be suppressed so demo links never leave the preview.

---

## L2-068: Global catalog search
**Traces to:** L1-027

The header search must index the whole catalog (foundations, 29 components, 7 dialog families, 8 pattern families — 45 entries), match case-insensitively on label, description, and type, cap results at 12, and support full keyboard operation.

**Acceptance Criteria:**
1. Given the search box (`aria-label="Search documentation"`, `aria-controls="search-results"`), when `restaurant` is typed, then the `role="listbox"` results panel must show exactly 2 results and activating the first must navigate to `/components/sd-restaurant-card/overview`.
2. Given a query matching more than 12 entries, when results render, then at most 12 must be shown, each as a `role="option"` link with icon, label, description, and type badge (`System`, `Component`, `Dialog`, or `Pattern`).
3. Given a query matching nothing, when results render, then the exact empty state `No catalog entries match that search.` must be shown; given the query is cleared, then the panel must hide and the input's `aria-expanded` must be `false`.
4. Given focus is not in an input, textarea, or select, when `/` is pressed, then the search box must receive focus without typing the character; when `Escape` is pressed in the box, then the query must clear and the box must blur; when `Enter` is pressed, then the first result must be activated.
5. Given a result is activated or any outside click occurs, when handled, then the results panel must close, and result activation must additionally clear the query and navigate client-side.

---

## L2-069: Keyboard reachability and visible focus
**Traces to:** L1-028

Every catalog interaction must be operable by keyboard alone, with a skip link, programmatic focus management on navigation, and a visible focus indicator drawn from the token palette.

**Acceptance Criteria:**
1. Given a keyboard user presses `Tab` on page load, when the first focusable element is reached, then a `Skip to content` link targeting `#main` (`tabindex="-1"`) must appear.
2. Given any client-side navigation, when the new route renders, then focus must be placed on `#main` with `preventScroll` after an instant scroll to top.
3. Given any focusable documentation control (button, link, input, select, textarea), when it receives keyboard focus, then a `3px solid var(--sd-sun)` outline with `3px` offset must be visible (`:focus-visible`).
4. Given a 390 px viewport, when the navigation drawer, tab links, playground controls, viewport buttons, and dialog launchers are driven with `Enter`/`Escape` and form-control keys only, then every flow must complete without a pointer.

---

## L2-070: Polite live-region announcements
**Traces to:** L1-028

Every non-navigational action must be announced through the single `#status` live region, rendered as a transient toast, so assistive technology hears state changes that are otherwise only visual.

**Acceptance Criteria:**
1. Given the document shell, when inspected, then `#status` must carry `role="status"` and `aria-live="polite"`, and each announcement must set its text, add the `show` class, and remove `show` after 1800 ms, resetting the timer if a new announcement arrives first.
2. Given the corresponding actions, when performed, then these exact messages must be announced: `HTML copied` / `CSS copied` / `JS copied` (code copy, active tab uppercased), `Link copied` (share-dialog copy rows), `Playground reset`, `Dialog opened`, `Dialog cancelled`, `Action confirmed`, `Dialog closed`, `Toggle on` / `Toggle off` (`sd-toggle` change), `Voted yes` / `Voted no` (`sd-vote-row` click), and `Mobile preview selected` / `Tablet preview selected` / `Desktop preview selected`.

---

## L2-071: Landmark and state semantics
**Traces to:** L1-028

Navigation, breadcrumbs, tabs, search, dialogs, and the menu button must expose their state through ARIA so the current location and each control's condition are programmatically determinable.

**Acceptance Criteria:**
1. Given the sidebar (`aria-label="Documentation navigation"`), when a route is active, then exactly the exact-match link must carry `aria-current="page"`.
2. Given a detail page breadcrumb, when inspected, then it must be a `nav` labelled `Breadcrumb` whose terminal crumb carries `aria-current="page"` and whose `/` separators are `aria-hidden="true"`.
3. Given the code panel's tabs, when a tab is switched, then `aria-selected` must be `true` on the active tab and `false` on the others within the `role="tablist"`.
4. Given the search input, when results open or close, then its `aria-expanded` must track the panel and options must be exposed as `role="option"` inside the `role="listbox"` panel.
5. Given the `#menu` button, when the drawer toggles, then `aria-expanded` and its accessible label (`Open documentation navigation` / `Close documentation navigation`) must both update, and `aria-controls="docs-nav"` must be present.
6. Given a live launched dialog, when inspected, then it must be exposed with `role="dialog"` and an accessible name from its title (e.g. `Sign out?`); pattern iframes must carry descriptive `title` attributes naming the pattern and scenario.

---

## L2-072: Folder self-containment
**Traces to:** L1-029

Nothing inside `design-system/` may depend on or reference the rest of the repository: no forbidden path strings in any validated file, no relative import escaping the folder, no HTML reference to a missing or external file, and no package script that walks above the folder root.

**Acceptance Criteria:**
1. Given every `.css`, `.html`, `.js`, `.mjs`, `.json`, and `.md` file in the folder (excluding build and test output directories), when scanned with path separators normalized, then none may contain the forbidden references `docs/mocks`, `../design-system`, `../frontend`, or `frontend/projects`.
2. Given any relative `import` in a `.js`/`.mjs` file, when resolved, then the target must remain inside the design-system folder.
3. Given every `href`/`src` in `index.html` and `preview.html` that is not a fragment or absolute-scheme URL, when resolved, then it must point at an existing file inside the folder.
4. Given `package.json`, when inspected, then the scripts `start`, `build`, `preview`, `serve:test`, `validate`, `test:browser`, and `test` must all exist and none may contain `..`.
5. Given `assets/catalog-content.js`, when inspected, then it must define the fixture builders `dialogMarkup`, `patternMarkup`, and `componentMarkup` locally — dialogs and pattern screens are composed from in-folder assets, never imported from the application.

---

## L2-073: Contract validation gate
**Traces to:** L1-030

`npm run validate` must execute `scripts/validate.mjs`, which enforces the manifest, parity, token, and self-containment invariants plus the presence of the key documentation renderers, failing loudly and blocking the browser suite when anything drifts.

**Acceptance Criteria:**
1. Given the folder is in its committed state, when `npm run validate` runs, then it must exit 0 and print exactly `Validated standalone schema v3: 29 components, 18 dialog scenarios, 40 responsive pattern states, and local-only assets.`.
2. Given any invariant is violated (wrong counts, undocumented observed attribute, registry drift, forbidden reference, second token file, missing script), when validation runs, then it must print one `- ` bulleted message per failure to stderr and exit with code 1.
3. Given `assets/docs.js`, when validated, then it must contain the renderers `renderPlayground`, `renderVariantMatrix`, `renderDialogExamples`, `renderedPatternScenario`, and `renderFoundations`.
4. Given `npm test` runs, when the scripts execute, then validation must run before the Playwright suite so contract violations fail the pipeline without launching a browser.

---

## L2-074: Three-viewport browser test gate
**Traces to:** L1-030

The Playwright suite must exercise the built catalog (not the dev server) across mobile, tablet, and desktop viewports, including a health sweep asserting zero console errors, zero failed requests, and no horizontal overflow.

**Acceptance Criteria:**
1. Given `playwright.config.js`, when inspected, then it must define three projects — `mobile` 390×844, `tablet` 820×1180, `desktop` 1440×900 — and a `webServer` running `npm run serve:test` (build then preview) at `http://127.0.0.1:4174/`.
2. Given the suite runs, when the health test executes, then it must visit `/`, `/components`, `/dialogs`, `/patterns`, `/foundations`, and `/components/sd-button/examples` and assert zero console errors, zero page errors, zero non-aborted failed requests, and `document.documentElement.scrollWidth === innerWidth` at every viewport.
3. Given the mobile drawer test, when the project viewport is not 390 px wide, then it must skip; all other tests must run in all three projects.
4. Given any test in the 11-test suite fails, when `npm test` runs, then the command must exit non-zero.

---

## L2-075: CI deployment pipeline
**Traces to:** L1-030

`.github/workflows/deploy-design-system.yml` must deploy the catalog to its own Azure Static Web App only after the validation and browser gates pass, triggered solely by changes that can affect it.

**Acceptance Criteria:**
1. Given the workflow triggers, when inspected, then it must run on `workflow_dispatch` and on pushes to `main` filtered to the paths `design-system/**` and `.github/workflows/deploy-design-system.yml`, with job permissions restricted to `contents: read`.
2. Given the job steps, when executed, then they must run in order — checkout, Node 22 setup, `npm ci`, `npx playwright install --with-deps chromium`, `npm test` — all with `working-directory: design-system`, and the deploy step must be unreachable if any earlier step fails.
3. Given the deploy step, when executed, then it must use `Azure/static-web-apps-deploy@v1` with `action: upload`, `app_location: design-system/dist`, `skip_app_build: true`, `skip_api_build: true`, authenticated by the `SWA_DESIGN_SYSTEM_DEPLOYMENT_TOKEN` secret.
4. Given `npm test` has run, when the deploy step uploads, then `design-system/dist` must contain the fresh build produced by the test step's web server (`serve:test` runs `npm run build` first).

---

## L2-076: Static hosting configuration
**Traces to:** L1-030

The deployed site's `staticwebapp.config.json` must preserve SPA deep links while serving assets, the manifest, and the preview page as raw files, must return honest 404s, and must apply security response headers globally.

**Acceptance Criteria:**
1. Given a request for a client route such as `/components/sd-button/api`, when served, then `navigationFallback` must rewrite it to `/index.html`; given a request under `/assets/*`, for `/component-manifest.json`, or for `/preview.html`, then the file must be served directly without rewriting.
2. Given a request for a missing static resource, when the host responds 404, then the body must be `/404.html` with status code 404 preserved.
3. Given any response, when its headers are inspected, then `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, and `Permissions-Policy: camera=(), microphone=(), geolocation=()` must be present.
4. Given `/component-manifest.json` is fetched (the header's `Manifest ↗` link), when served, then its content type must be `application/json`, and the Vite build must have copied the config, manifest, `404.html`, token/docs stylesheets, and the `assets/components/` tree into `dist/` unhashed.

---

## Observed gaps (design system)

Non-normative. The following are verified absences in the as-built catalog, recorded here so future requirements can close them deliberately rather than by accident. They are not requirements and have no acceptance criteria:

- **No reduced-motion handling.** No `prefers-reduced-motion` query exists anywhere in `design-system/assets/`; the dialog `rise` animation and loading pulse always run.
- **No dark color scheme.** No `prefers-color-scheme` query or theme attribute exists; the catalog commits to its single warm-light palette.
- **No Content-Security-Policy header.** `staticwebapp.config.json` sets nosniff, referrer, and permissions policies but no CSP.
